export type BlockShape = number[][];
export type BlockPosition = { x: number; y: number };
export type CellPosition = { x: number; y: number };
export type ServiceCallbackProps = { score: number; isWin: boolean };
